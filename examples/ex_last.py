# Retrieves the last element(s) with examples for single and multiple elements.

# Getting the last element
array = [10, 20, 30, 40]
result = unicore.last(array)
print(result)  # Output: 40

# Getting the last 2 elements
result = unicore.last(array, 2)
print(result)  # Output: [30, 40]