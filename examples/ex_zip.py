# Combines elements from multiple arrays into tuples based on their index.
array1 = [1, 2, 3]
array2 = ["a", "b", "c"]
result = unicore.zip(array1, array2)
print(result)  # Output: [(1, "a"), (2, "b"), (3, "c")]